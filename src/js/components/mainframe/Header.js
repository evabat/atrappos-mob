import React from 'react';
import {AppContext} from "../../../App";

export const Header = (props) => {
    const {prop} = props;
    return (
        <header>
            {"Atrappos"}
        </header>
    );
};